# 🛍️ FRONTEND-SHOP

Este proyecto corresponde a la **estructura base de la interfaz de usuario (UI)** desarrollada con **Next.js**.  
Su propósito es servir como punto de partida para el desarrollo del frontend de una aplicación web conectada a una API externa.

---

## ⚙️ Ejecución

1. Instalar las dependencias:
   ```bash
   bun install
   ```
2. Crear en la raiz del proyecto un .env.local:
   ```bash
   NEXT_PUBLIC_API_URL=http://localhost:4000/api
   NODE_ENV=development
   ```
3. Levantar el servidor en modo desarollo:
   ```bash
   bun --bun run dev
   bun run dev
   bun dev
   ```
